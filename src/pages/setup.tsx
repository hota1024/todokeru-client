import { SetupContainer } from '@/components/SetupContainer'
import { NextPage } from 'next'

/**
 * Setup page.
 */
export const SetupPage: NextPage = () => {
  return <SetupContainer />
}

export default SetupPage
