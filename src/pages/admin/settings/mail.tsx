import { MailSettings } from '@/page-components/MailSettings/MailSettings'
import { NextPage } from 'next'

/**
 * MailSettings page.
 */
export const MailSettingsPage: NextPage = () => {
  return <MailSettings />
}

export default MailSettingsPage
