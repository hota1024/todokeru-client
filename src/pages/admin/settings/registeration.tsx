import { RegisterationSettings } from '@/page-components/RegisterationSettings/RegisterationSettings'
import { NextPage } from 'next'

/**
 * RegisterationSettings page.
 */
export const RegisterationSettingsPage: NextPage = () => {
  return <RegisterationSettings />
}

export default RegisterationSettingsPage
