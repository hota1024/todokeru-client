query students {
  students {
    id
    name
    surname
    group {
      id
      name
    }
    user {
      id
    }
  }
}

query student($id: String!) {
  student(id: $id) {
    id
    name
    surname
    group {
      id
      name
    }
    user {
      id
    }
  }
}
