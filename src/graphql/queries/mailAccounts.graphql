query mailAccounts {
  mailAccounts {
    id
    host
    port
    secure
    user
    isPrimary
    updatedAt
    createdAt
  }
}

query mailAccount($id: String!) {
  mailAccount(id: $id) {
    id
    host
    port
    secure
    user
    isPrimary
    updatedAt
    createdAt
  }
}

query hasPrimaryMailAccount {
  hasPrimaryMailAccount
}
