query mails {
  mails {
    id
    subject
    body
    groups {
      id
      name
    }
    wasSent
  }
}

query mail($id: String!) {
  mail(id: $id) {
    id
    subject
    body
    groups {
      id
      name
    }
    wasSent
  }
}
