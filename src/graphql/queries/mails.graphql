query mails {
  mails {
    id
    subject
    body
    groups {
      id
      name
    }
    wasSent
    createdAt
  }
}

query mail($id: String!) {
  mail(id: $id) {
    id
    subject
    body
    groups {
      id
      name
    }
    wasSent
    createdAt
  }
}
