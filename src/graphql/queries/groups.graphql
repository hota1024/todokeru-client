query groups {
  groups {
    id
    name
    students {
      id
    }
  }
}

query group($id: String!) {
  group(id: $id) {
    id
    name
    createdAt
    updatedAt
    students {
      id
      name
      surname
    }
  }
}
