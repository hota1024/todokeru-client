query groups {
  groups {
    id
    name
    order
    students {
      id
    }
  }
}

query group($id: String!) {
  group(id: $id) {
    id
    name
    createdAt
    updatedAt
    order
    students {
      id
      name
      surname
    }
  }
}
